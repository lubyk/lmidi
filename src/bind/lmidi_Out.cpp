/**
 *
 * MACHINE GENERATED FILE. DO NOT EDIT.
 *
 * Bindings for class Out
 *
 * This file has been generated by dub 2.2.0.
 */
#include "dub/dub.h"
#include "lmidi/Out.h"

using namespace lmidi;

/** lmidi::Out::Out()
 * include/lmidi/Out.h:44
 */
static int Out_Out(lua_State *L) {
  try {
    Out *retval__ = new Out();
    retval__->dub_pushobject(L, retval__, "lmidi.Out", true);
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "new: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "new: Unknown exception");
  }
  return dub::error(L);
}

/** lmidi::Out::~Out()
 * include/lmidi/Out.h:52
 */
static int Out__Out(lua_State *L) {
  try {
    DubUserdata *userdata = ((DubUserdata*)dub::checksdata_d(L, 1, "lmidi.Out"));
    if (userdata->gc) {
      Out *self = (Out *)userdata->ptr;
      delete self;
    }
    userdata->gc = false;
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "__gc: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "__gc: Unknown exception");
  }
  return dub::error(L);
}

/** int lmidi::Out::port() const
 * include/lmidi/Out.h:59
 */
static int Out_port(lua_State *L) {
  try {
    Out *self = *((Out **)dub::checksdata(L, 1, "lmidi.Out"));
    lua_pushnumber(L, self->port());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "port: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "port: Unknown exception");
  }
  return dub::error(L);
}

/** const char* lmidi::Out::portName() const
 * include/lmidi/Out.h:63
 */
static int Out_portName(lua_State *L) {
  try {
    Out *self = *((Out **)dub::checksdata(L, 1, "lmidi.Out"));
    lua_pushstring(L, self->portName());
    return 1;
  } catch (std::exception &e) {
    lua_pushfstring(L, "portName: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "portName: Unknown exception");
  }
  return dub::error(L);
}

/** void lmidi::Out::openPort(int port, lua_State *L)
 * include/lmidi/Out.h:67
 */
static int Out_openPort(lua_State *L) {
  try {
    Out *self = *((Out **)dub::checksdata(L, 1, "lmidi.Out"));
    int type__ = lua_type(L, 2);
    if (type__ == LUA_TNUMBER) {
      int port = dub::checkint(L, 2);
      self->openPort(port, L);
      return 0;
    } else {
      const char *port_name = dub::checkstring(L, 2);
      self->openPort(port_name, L);
      return 0;
    }
  } catch (std::exception &e) {
    lua_pushfstring(L, "openPort: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "openPort: Unknown exception");
  }
  return dub::error(L);
}

/** void lmidi::Out::virtualPort(const char *port_name, lua_State *L)
 * include/lmidi/Out.h:101
 */
static int Out_virtualPort(lua_State *L) {
  try {
    Out *self = *((Out **)dub::checksdata(L, 1, "lmidi.Out"));
    const char *port_name = dub::checkstring(L, 2);
    self->virtualPort(port_name, L);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "virtualPort: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "virtualPort: Unknown exception");
  }
  return dub::error(L);
}

/** void lmidi::Out::send(int a, int b, int c)
 * include/lmidi/Out.h:106
 */
static int Out_send(lua_State *L) {
  try {
    Out *self = *((Out **)dub::checksdata(L, 1, "lmidi.Out"));
    int a = dub::checkint(L, 2);
    int b = dub::checkint(L, 3);
    int c = dub::checkint(L, 4);
    self->send(a, b, c);
    return 0;
  } catch (std::exception &e) {
    lua_pushfstring(L, "send: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "send: Unknown exception");
  }
  return dub::error(L);
}

/** static LuaStackSize lmidi::Out::ports(lua_State *L)
 * include/lmidi/Out.h:116
 */
static int Out_ports(lua_State *L) {
  try {
    return Out::ports(L);
  } catch (std::exception &e) {
    lua_pushfstring(L, "ports: %s", e.what());
  } catch (...) {
    lua_pushfstring(L, "ports: Unknown exception");
  }
  return dub::error(L);
}



// --=============================================== __tostring
static int Out___tostring(lua_State *L) {
  Out *self = *((Out **)dub::checksdata_n(L, 1, "lmidi.Out"));
  lua_pushfstring(L, "lmidi.Out: %p (%s (%f))", self, self-> portName() , self-> port() );
  
  return 1;
}

// --=============================================== METHODS

static const struct luaL_Reg Out_member_methods[] = {
  { "new"          , Out_Out              },
  { "__gc"         , Out__Out             },
  { "port"         , Out_port             },
  { "portName"     , Out_portName         },
  { "openPort"     , Out_openPort         },
  { "virtualPort"  , Out_virtualPort      },
  { "send"         , Out_send             },
  { "ports"        , Out_ports            },
  { "__tostring"   , Out___tostring       },
  { "deleted"      , dub::isDeleted       },
  { NULL, NULL},
};


extern "C" int luaopen_lmidi_Out(lua_State *L)
{
  // Create the metatable which will contain all the member methods
  luaL_newmetatable(L, "lmidi.Out");
  // <mt>

  // register member methods
  dub::fregister(L, Out_member_methods);
  // setup meta-table
  dub::setup(L, "lmidi.Out");
  // <mt>
  return 1;
}
